function [vh, imgh] = homogFPS(cmslData, mp, fmdl)
% homogFPS takes model parameters, builds geometry and returns the solution
% of the forward problem for a homogenous case.
   

end

% plotify(fmdl);

function plotify(fmdl)
show_fem(fmdl); 
xlabel('x')
zlabel('z')
end