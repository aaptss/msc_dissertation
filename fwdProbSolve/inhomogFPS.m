function [vi, imgi] = inhomogFPS(imgh, fmdl)
% inhomogFPS takes the image of homogFPS case, adds needed parameters and solves 
% for an inhhomogenous case.
    

end

% plotify(fmdl);

function plotify(fmdl)
show_fem(fmdl); 
xlabel('x')
zlabel('z')
end